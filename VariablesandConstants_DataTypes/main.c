#include <stdio.h>
#include <stdlib.h>

int main()
{
//    variable: vary or change
//    variable: container: it store any type of data

    const int a = 10; // declaration/Creation and initialization/Assignment

    printf("A is: %d\n", a); // \n- escape character, new line or enter

    //constant: fixed value

    //a = 50; // error, as a is constant now.

    printf("A is: %d", a);

    return 0;
}
